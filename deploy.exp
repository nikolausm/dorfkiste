#!/usr/bin/expect -f
set timeout 300
spawn ssh dorfkiste@194.164.199.151 "cd /home/dorfkiste/dorfkiste && git pull origin master && docker compose down && docker compose build && docker compose up -d"
expect "password:"
send "Passw0rt!\r"
expect eof
